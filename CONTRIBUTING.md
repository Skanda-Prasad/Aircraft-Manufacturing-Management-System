# Contributing

Thanks for considering contributing. Basic guidelines:

- Fork the repo and open a PR against `main`.
- Keep changes small and focused.
- Run tests locally with `pytest` before submitting.
- Document public API changes in `README.md`.
